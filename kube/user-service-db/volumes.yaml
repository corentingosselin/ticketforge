apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: user-mysql
spec:
  volumeName: users-data
  resources:
    requests:
      storage: 8Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
